pipeline {
  agent any

  stages {
    stage('install') {
      steps {
        // your actual steps here, e.g.:
        sh 'npm install'
      }
    }

    stage('build') {
      steps {
        sh 'npm run build'
      }
    }

    // more stages...
  }

  post {
    always {
      echo 'Cleaning up!'
    }
  }
}
